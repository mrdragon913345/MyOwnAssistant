# Agent Rules

- NEVER push GitHub updates to the main bot or API repositories without getting explicit confirmation from the user first. Always ask before running any push scripts (e.g., `push_bot_to_github.py` or `push_api_clean.py`).
